# Public source validation — 2026-09-22

以下结果针对本次公开源码副本，在独立本地测试目录运行。没有读取已有业务数据库、使用真实账号，或向业务服务发压。

| 检查 | 实际结果 |
| --- | --- |
| 后端维护套件 `deployment/test-runner/run_catalog_tests.py` | 470项：430通过、40跳过、0失败；跳过项不算通过 |
| 前端性能测试组件与逻辑 | 317项通过，0失败、0跳过 |
| k6脚本时长、文本断言、执行策略和WebSocket合同 | 55项通过，0失败、0跳过 |
| 独立部署工具静态测试 | 33项通过，包括6项公开版运行器检查测试；没有连接Docker |
| 前端生产构建 | 通过；保留大体积chunk警告 |
| 本次修改的前端JS/MJS静态检查 | 通过：语法、不可达代码、typeof及意外多行检查；不代表全仓所有风格规则通过 |
| Python语法检查 | 通过；原有无效转义写法的SyntaxWarning仍保留 |
| 公开文件检查 | 排除公司CI/Kubernetes配置、私有Git历史、真实接口规范、账号、数据库、日志及构建产物；对发布文件检查已知公司标识与凭据格式 |

## 完整旧套件仍有问题

首次运行完整 `apps.perf_testing` 套件共945项：5项断言失败、14项错误、64项跳过，不能写成全量通过。

其中1项来自本次合成OpenAPI样本未包含待人工准备的参数类型；已增加合成复杂查询参数，并在维护套件中复验通过。其余18项在未修改的原始源码中逐项重新运行，均复现相同失败，现阶段保留：

- `test_full_flow`：3项，测试直接调用视图/鉴权时缺少完整请求上下文。
- `test_consumer_auth`：2项，测试用户或执行对象缺少当前权限检查所需字段。
- `test_jmeter_engine`：2项，旧测试要求固定盘符，与本机环境不符。
- `test_k6_integration`：5项，旧测试替身缺少当前执行入口需要的步骤或执行策略字段。
- `test_script_ref_resolution`：5项，直接调用序列化校验时没有设置引擎上下文。
- `test_k6_throughput`：1项，仍断言旧适配器版本0.7.1，当前源码为0.12.1。

这些问题没有通过删除测试、伪造通过或修改业务权限来掩盖。修正合成样本后没有重复执行整个945项套件；上述维护套件和原始版本定向对照是分别记录的结果。

需要复现完整套件时，先按README初始化独立本地密钥，再从 `testhub/` 运行：

```sh
python manage.py test apps.perf_testing --settings=backend.pressure_settings --noinput
```

公开版Linux镜像、Docker部署和目标业务容量尚未重新验收；源码可构建与单元测试通过不能代替这些结论。公司环境的真实压测报告和资源数据不随个人仓库发布。
